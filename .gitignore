# JUCE / Projucer generated trees — regenerated from Sysex77.jucer via Projucer.
# (The prebuilt macOS app is tracked explicitly under dist/.)
/JuceLibraryCode/
/Builds/

# CMake build trees (build via ./build.sh)
/build/
/build-cmake/

# macOS
.DS_Store

# Documentation de référence (ne pas committer)
/docs/
